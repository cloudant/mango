{application, mango, [
    {description, "MongoDB API compatibility layer for Cloudant"},
    {vsn, git},
    {registered, []},
    {applications, [
        kernel,
        stdlib,
        couch_log,
        config,
        fabric
    ]}
]}.
